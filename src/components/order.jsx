import React, { Component } from "react";

class Order extends Component {
  state = {
    items: [],
  };

  render() {
    return;
  }

  processOrder(Order) {
    // will handle logic for order processing
    // taking input from user
    // printing reciept
  }
}

export default Order;
