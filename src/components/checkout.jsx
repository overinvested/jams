import React, { Component } from "react";
import Order from "./order";

class CheckOut extends Component {
  state = {
    cart: [],
  };

  render() {
    return;
  }

  renderOrder() {
    // renders the current order for checkout
  }
}

export default CheckOut;
